sudo: false
language: node_js
node_js:
  - 10 # use nodejs v10 LTS
cache: npm
branches:
  only:
    - master # build master branch only
script:
  - hexo generate # generate static files
deploy:
  provider: pages
  skip-cleanup: true
  github-token: c3fe4faa785c3270c960e1495fb5ab50a17f6c7f
  keep-history: true
  on:
    branch: master
  local-dir: public